comment=Fixed issue with multiple src files in the workspace.  If only 1 exists will automatically open and compile that one.  If there are mulitples it will attempt to compile the active window.  If it is not an .src file errors will occur like before.  
